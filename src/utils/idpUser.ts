// import Keycloak from 'keycloak-js';

// const keycloak = new Keycloak({
//     url: 'http://keycloak-server${kc_base_path}',
//     realm: 'myrealm',
//     clientId: 'myapp'
// });

// try {
//     const authenticated = await keycloak.init({
//         pkceMethod: authConfig.keycloak.initOptions.pkceMethod,
//         redirectUri: window.location.href,
//         onLoad: authConfig.keycloak.initOptions.onLoad,
//         silentCheckSsoRedirectUri: window.location.origin + authConfig.keycloak.initOptions.silentCheckSsoRedirectUri,
//       });
//     console.log(`User is ${authenticated ? 'authenticated' : 'not authenticated'}`);
// } catch (error) {
//     console.error('Failed to initialize adapter:', error);
// }


